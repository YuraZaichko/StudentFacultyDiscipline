@model IEnumerable<StudentGroupFacultyApp.Models.FacultyDiscipline>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Discipline.DisciplineName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Discipline.DisciplineName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FacultyDisciplineId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FacultyDisciplineId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FacultyDisciplineId })
        </td>
    </tr>
}

</table>
